<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyTime.Views"
             x:Class="MyTime.Views.OverviewPage"
             Title="Overview">

    <StackLayout Padding="30" Spacing="20">
        <Button Text="Arbeitsstart" Command="{Binding NavigateToStartWorkPageCommand}"/>
        <Button Text="Arbeitsende" Command="{Binding NavigateToEndWorkPageCommand}"/>
        <Button Text="Zeitkorrektur" Command="{Binding NavigateToCorrectTimePageCommand}"/>
        <Button Text="Zeitnachtrag" Command="{Binding NavigateToAddTimePageCommand}"/>
        <Button Text="Übersicht" Command="{Binding NavigateToOverviewDetailsPageCommand}"/>
        <Button Text="Logout" Command="{Binding LogoutCommand}"/>
    </StackLayout>
</ContentPage>
