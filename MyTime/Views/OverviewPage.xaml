<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyTime.Views"
             x:Class="MyTime.Views.OverviewPage"
             Title="Overview">

    <StackLayout Padding="30" Spacing="20">
        <Button Text="Arbeitsstart" Command="{Binding StartWorkCommand}"/>
        <Button Text="Arbeitsende" Command="{Binding EndWorkCommand}"/>
        <Button Text="Zeitkorrektur" Command="{Binding CorrectTimeCommand}"/>
        <Button Text="Zeitnachtrag" Command="{Binding AddTimeCommand}"/>
        <Button Text="Übersicht" Command="{Binding OverviewCommand}"/>
        <Button Text="Logout" Command="{Binding LogoutCommand}"/>
    </StackLayout>
</ContentPage>
